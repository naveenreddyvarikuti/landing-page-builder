# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0

# Vector store (used directly; ships a built-in all-MiniLM-L6-v2 embedder)
chromadb>=0.5.0

# State schema
pydantic>=2.0.0

# Environment
python-dotenv>=1.0.0

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Parsing HTML and CSS for RAG AST-based chunking
tree-sitter>=0.22.0
tree-sitter-html
tree-sitter-css
tree-sitter-javascript>=0.23.0
